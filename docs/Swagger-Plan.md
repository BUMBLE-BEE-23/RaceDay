# RaceDay Swagger Plan

Swagger/OpenAPI will document and test all implemented API endpoints.

The Swagger interface will include:
- Authentication endpoints
- Profile endpoints
- Event endpoints
- Event type endpoints
- Category endpoints
- Enrolment endpoints
- Result endpoints

Each endpoint will document its HTTP method, route, required authentication, request data and expected response.

Protected endpoints will support authenticated testing using the API authentication mechanism.
